// Provider configurations live here. The Terraform block (backend and
// required_providers) is defined once in versions.tf to avoid duplicates.
// Add specific provider settings below if/when needed.

// Example:
provider "aws" {
  region = "eu-west-3"
}
